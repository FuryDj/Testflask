git --version

git config --global user.name "FuryDj"

git config --global user.email "iamdjordjesimic@gmail.com"
ABB12dbb!"

git init
to make a repository in current path

git status

git add myapp.py
adding to the repository

git add *.txt
adding all files that ends with .txt

git commit -m"Added first test"

git remote add origin https://github.com/FuryDj/Testflask.git
origin is my online repository
with this we establish connection between local and online repository

git push -u origin master
-u mean remeber commands so next time we can type just git push
